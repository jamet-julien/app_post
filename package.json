{
  "name": "ArmstrongPost",
  "productName": "ArmstrongPost",
  "version": "0.0.1",
  "description": "example menubar electron app",
  "dependencies": {},
  "devDependencies": {
    "electron-positioner": "^3.0.0",
    "extend": "^3.0.0"
  },
  "main": "index.js",
  "scripts": {
    "build-mac" : "electron-packager . ArmstrongPost --overwrite --platform=darwin --arch=x64 --version=0.36.0 --icon=Icon.icns",
    "build-win" : "electron-packager . ArmstrongPost --overwrite --platform=win32 --arch=ia32",
    "build-all" : "electron-packager . --all",
    "start"     : "electron ."
  }
}
